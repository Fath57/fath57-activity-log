export const AUDIT_MODULE_OPTIONS = 'AUDIT_MODULE_OPTIONS';

/** The resolved adapter and the audit port read off it. See feed.constants. */
export const AUDIT_ADAPTER = 'AUDIT_ADAPTER';
export const AUDIT_READER = 'AUDIT_READER';
